SELECT CM.NAME,QT.S_ORDER_NO,QT.AVG_QTY,QT.ORDER_VALUE
 FROM CLIENT_MASTER CM,
(SELECT S_ORDER_NO,AVG(QTY_ORDERED) "AVG_QTY",
SUM(QTY_ORDERED*PRODUCT_RATE) "ORDER_VALUE" FROM
SALES_ORDER_DETAILS 
GROUP BY S_ORDER_NO
HAVING SUM(QTY_ORDERED*PRODUCT_RATE)<=15000) QT;