DROP TABLE STUD;

CREATE TABLE STUD
(GRNO NUMBER(4),
NAME VARCHAR2(30));


CREATE OR REPLACE TRIGGER weekday
BEFORE INSERT OR UPDATE OR DELETE
ON stud FOR EACH ROW
DECLARE
	V_DAY VARCHAR2(10);
BEGIN
	SELECT TO_CHAR(SYSDATE,'DY') INTO V_DAY FROM DUAL;
	IF V_DAY='SUN'OR V_DAY='SAT' THEN
		RAISE_APPLICATION_ERROR(-20000,'ITS HOLIDAY..');
	END IF;
	
END;
/

INSERT INTO STUD VALUES(1,'A');