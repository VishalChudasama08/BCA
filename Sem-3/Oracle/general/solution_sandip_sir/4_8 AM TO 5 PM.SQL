DROP TABLE STUD;

CREATE TABLE STUD
(GRNO NUMBER(4),
NAME VARCHAR2(30));


CREATE OR REPLACE TRIGGER WORKING_HOURS
BEFORE INSERT OR UPDATE OR DELETE
ON stud FOR EACH ROW
DECLARE
	V_TIME VARCHAR2(10);
BEGIN
	SELECT TO_CHAR(SYSDATE,'HH24:MI') INTO V_TIME FROM DUAL;
	IF V_TIME NOT BETWEEN '08:00' AND '17:00' THEN
		RAISE_APPLICATION_ERROR(-20000,'YOU CAN CHANGE DATA BETWEEN 8 TO 5..');
	END IF;
	
END;
/

INSERT INTO STUD VALUES(1,'A');