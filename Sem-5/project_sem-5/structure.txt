Login’s
	•	User
		-	Normal user
		-	Clint 
	•	Admin
	
Normal User 
=   View all project theatres and there’s movie 
=   Movie 
	-	Movie name 
	-	Image
	-	Streaming time
table - use
1. id (unic id)
2. name
3. email/username (email is username)
4. password
5. mobile number
6. date (set the date the user first logs in)
7. modify date (if user modify username and password than save that day date)

CREATE TABLE `users` (
  `id` INT(11) UNSIGNED NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(255) NOT NULL,
  `email` VARCHAR(255) NOT NULL UNIQUE,
  `password` VARCHAR(255) NOT NULL,
  `mobile_number` VARCHAR(15) NOT NULL,
  `date` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `modify_date` TIMESTAMP NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
);


table - movies
1. id (unic id)
2. name 
3. duration (movie length)
4. genre (e.g. action, comedy, romans...)
5. rating 
6. description  
7. created date
8
. updated date


CREATE TABLE IF NOT EXISTS movies (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    genre VARCHAR(100),
    duration VARCHAR(20),
    rating VARCHAR(10),
    image_location VARCHAR(255),
    description TEXT,
    release_date DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP
);
INSERT INTO movies (title, genre, duration, rating, description, release_date)
VALUES ('Inception', 'Sci-Fi', 148, 'PG-13', 'A mind-bending thriller', '2010-07-16');

CREATE TABLE IF NOT EXISTS showtimes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    movie_id INT NOT NULL,
    show_date DATE NOT NULL,
    show_time TIME NOT NULL,
    available_seats INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (movie_id) REFERENCES movies(id) ON DELETE CASCADE
);
INSERT INTO showtimes (movie_id, show_date, show_time, available_seats)
VALUES (1, '2024-06-01', '18:00:00', 100);


table - theatres
1. id (unic id)
2. the_name
3. total_seats (total seats in this theatre) (int) // if need than make this field
4. seats_structure (2d array)
3. total_times (in one day total streaming movies number) (int)
4. time (in one day total streaming movies number) (array)
5. the_location (theatre location)


